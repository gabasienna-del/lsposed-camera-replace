LSPosed Camera Replace Module
=============================
Project skeleton for an LSPosed module that replaces camera output with a chosen image.
See instructions in the manifest and code comments.
